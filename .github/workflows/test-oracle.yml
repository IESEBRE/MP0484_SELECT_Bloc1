name: Test Oracle SQL

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Start Oracle XE container
        run: |
          docker run -d             --name oracle-db             -p 1521:1521             -e ORACLE_PASSWORD=oracle             -v ${{ github.workspace }}:/workspace             gvenzl/oracle-xe

      - name: Run SQL tests
        run: bash tests/test_sql_oracle.sh
